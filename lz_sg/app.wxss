/**app.wxss**/
page {
  background-color: #1a1a2e;
  color: #ecf0f1;
}

/* 全局重置 */
view, text, button {
  box-sizing: border-box;
}

/* 全局按钮样式 */
button {
  padding: 0;
  margin: 0;
}

button::after {
  border: none;
}
