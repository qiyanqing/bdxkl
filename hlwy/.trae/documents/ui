# **社区闲置快换小程序UI/UX设计规范文档**

## **一、设计理念与设计语言**

### **核心设计理念**
- **邻里感**：营造温暖、友好的社区氛围，强调邻里互助的亲近感
- **清爽高效**：界面简洁明了，操作流程直观，让用户快速完成闲置发布与交易
- **真实可信**：设计元素强调真实感，建立用户信任基础
- **环保简约**：体现闲置循环的环保理念，避免过度设计

### **设计语言关键词**
**温暖 · 清晰 · 友好 · 实用**

## **二、整体视觉风格**

### **1. 色彩体系 (Color System)**

#### **主色 (Primary Colors)**
- **品牌绿** `#07C160` - 微信生态关联色，代表信任与环保
- **辅助蓝** `#2A6EFF` - 科技感、可靠感（用于重要操作）
- **温暖橙** `#FF9500` - 活力、友好（用于提醒、互动元素）

#### **中性色 (Neutral Colors)**
- **标题黑** `#333333` - 主要文字、重要标题
- **正文灰** `#666666` - 正文内容、次要信息
- **辅助灰** `#999999` - 辅助文字、占位符
- **分割线** `#EEEEEE` - 边框、分割线、卡片阴影
- **背景灰** `#F8F8F8` - 页面背景、模块背景

#### **功能色 (Functional Colors)**
- **成功绿** `#34C759` - 成功状态、确认信息
- **警告黄** `#FFCC00` - 警告、提醒
- **错误红** `#FF3B30` - 错误、重要警告
- **链接蓝** `#007AFF` - 可点击链接

### **2. 字体规范 (Typography)**

#### **字体选择**
- **中文**：优先使用系统默认字体，确保清晰可读
- **英数**：`-apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif`
- **后备方案**：`PingFang SC, Microsoft YaHei, sans-serif`

#### **字号与行高**
| 层级 | 字体大小 | 行高 | 字重 | 使用场景 |
|------|----------|------|------|----------|
| H1 | 20px | 28px | Bold | 页面主标题 |
| H2 | 18px | 26px | Semibold | 模块标题、重要按钮 |
| H3 | 16px | 24px | Semibold | 卡片标题、次要标题 |
| Body1 | 14px | 22px | Regular | 正文内容 |
| Body2 | 13px | 20px | Regular | 辅助说明文字 |
| Caption | 12px | 18px | Regular | 标签、极小文字 |
| Button | 15px | 22px | Medium | 按钮文字 |

### **3. 间距系统 (Spacing System)**

#### **基础间距单位**：4px
所有间距使用4px的倍数，保持视觉节奏统一

| 间距名称 | 数值 | 使用场景 |
|---------|------|----------|
| XS | 4px | 图标与文字间距、微小组件内边距 |
| S | 8px | 列表项间距、小模块内边距 |
| M | 12px | 卡片内部元素间距、标准内边距 |
| L | 16px | 模块间距、卡片间距离 |
| XL | 24px | 大模块间距、页面上下边距 |
| XXL | 32px | 页面级间距、重要分隔 |

### **4. 圆角系统 (Border Radius)**

| 圆角类型 | 数值 | 使用场景 |
|---------|------|----------|
| 无圆角 | 0 | 分割线、全屏元素 |
| 小圆角 | 4px | 按钮、输入框、小标签 |
| 中圆角 | 8px | 卡片、弹窗、头像 |
| 大圆角 | 12px | 大卡片、图片容器 |
| 全圆角 | 50% | 圆形头像、圆形按钮 |

### **5. 阴影系统 (Shadow System)**

| 阴影层级 | 数值 | 使用场景 |
|---------|------|----------|
| 无阴影 | none | 背景元素、分割线 |
| 轻微阴影 | 0 2px 8px rgba(0,0,0,0.04) | 卡片悬停、轻微层次 |
| 标准阴影 | 0 4px 12px rgba(0,0,0,0.08) | 卡片、浮层 |
| 明显阴影 | 0 8px 24px rgba(0,0,0,0.12) | 弹窗、重要浮层 |

## **三、核心组件规范**

### **1. 按钮 (Buttons)**

#### **主要按钮 (Primary Button)**
```css
/* 大号按钮 */
.btn-primary-lg {
  background: #07C160;
  color: #FFFFFF;
  height: 48px;
  border-radius: 8px;
  font-size: 15px;
  font-weight: 500;
  padding: 0 24px;
}

/* 标准按钮 */
.btn-primary {
  background: #07C160;
  color: #FFFFFF;
  height: 44px;
  border-radius: 6px;
  font-size: 14px;
  padding: 0 20px;
}
```

#### **次要按钮 (Secondary Button)**
```css
.btn-secondary {
  background: #FFFFFF;
  color: #07C160;
  height: 44px;
  border-radius: 6px;
  border: 1px solid #07C160;
  font-size: 14px;
  padding: 0 20px;
}
```

#### **文本按钮 (Text Button)**
```css
.btn-text {
  background: transparent;
  color: #2A6EFF;
  height: auto;
  font-size: 14px;
  padding: 4px 8px;
}
```

#### **按钮状态**
- **正常状态**：如上样式
- **悬停状态**：透明度降低到90%
- **按下状态**：使用`scale(0.98)`轻微缩放
- **禁用状态**：`background: #EEEEEE; color: #999999;`

### **2. 输入框 (Input Fields)**

#### **标准输入框**
```css
.input-standard {
  height: 44px;
  background: #FFFFFF;
  border: 1px solid #EEEEEE;
  border-radius: 6px;
  padding: 0 12px;
  font-size: 14px;
  color: #333333;
}

.input-standard:focus {
  border-color: #07C160;
  box-shadow: 0 0 0 2px rgba(7, 193, 96, 0.1);
}
```

#### **搜索框**
```css
.search-input {
  height: 36px;
  background: #F5F5F5;
  border-radius: 18px;
  padding: 0 16px;
  font-size: 14px;
  color: #333333;
  display: flex;
  align-items: center;
}

.search-input .icon {
  color: #999999;
  margin-right: 8px;
}
```

### **3. 卡片 (Cards)**

#### **物品卡片**
```css
.item-card {
  background: #FFFFFF;
  border-radius: 8px;
  padding: 16px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  margin-bottom: 12px;
}

/* 卡片布局结构 */
.item-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.item-card-images {
  width: 100%;
  height: 160px;
  border-radius: 6px;
  background: #F8F8F8;
  margin-bottom: 12px;
  overflow: hidden;
}

.item-card-info {
  margin-bottom: 12px;
}

.item-card-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 12px;
  border-top: 1px solid #EEEEEE;
}
```

### **4. 标签 (Tags)**

#### **场景标签**
```css
.tag-scene {
  display: inline-flex;
  align-items: center;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
}

.tag-community {
  background: rgba(7, 193, 96, 0.1);
  color: #07C160;
}

.tag-company {
  background: rgba(42, 110, 255, 0.1);
  color: #2A6EFF;
}
```

#### **状态标签**
```css
.tag-status {
  padding: 2px 6px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
}

.tag-available {
  background: rgba(52, 199, 89, 0.1);
  color: #34C759;
}

.tag-trading {
  background: rgba(255, 149, 0, 0.1);
  color: #FF9500;
}

.tag-sold {
  background: rgba(153, 153, 153, 0.1);
  color: #999999;
}
```

### **5. 导航栏 (Navigation)**

#### **顶部导航栏**
```css
.navbar {
  height: 44px;
  background: #FFFFFF;
  display: flex;
  align-items: center;
  padding: 0 16px;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  box-shadow: 0 1px 0 0 #EEEEEE;
}

.navbar-title {
  font-size: 18px;
  font-weight: 600;
  color: #333333;
  flex: 1;
  text-align: center;
}

.navbar-back {
  color: #333333;
  font-size: 16px;
}

.navbar-action {
  color: #2A6EFF;
  font-size: 14px;
}
```

#### **底部标签栏**
```css
.tab-bar {
  height: 56px;
  background: #FFFFFF;
  display: flex;
  justify-content: space-around;
  align-items: center;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  box-shadow: 0 -1px 0 0 #EEEEEE;
  padding-bottom: env(safe-area-inset-bottom, 0);
}

.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
}

.tab-icon {
  font-size: 22px;
  margin-bottom: 2px;
}

.tab-text {
  font-size: 10px;
}

/* 激活状态 */
.tab-active .tab-icon,
.tab-active .tab-text {
  color: #07C160;
}

/* 未激活状态 */
.tab-inactive .tab-icon,
.tab-inactive .tab-text {
  color: #999999;
}
```

## **四、页面布局模板**

### **1. 首页布局 (Home Page)**
```
┌─────────────────────────────────┐
│ [返回] 闲置快换 [发布按钮] │ ← 导航栏
├─────────────────────────────────┤
│ [搜索框] │
├─────────────────────────────────┤
│ [场景切换Tabs] │
├─────────────────────────────────┤
│ [筛选条件：分类/价格/距离] │
├─────────────────────────────────┤
│ ┌─────────────────────┐ │
│ │ 物品卡片 │ │
│ │ ┌─────────────┐ │ │
│ │ │ 图片 │ │ │
│ │ └─────────────┘ │ │
│ │ 标题 │ │
│ │ 价格 · 标签 │ │
│ │ 距离 · 发布时间 │ │
│ └─────────────────────┘ │
│ │
│ [无限滚动列表] │
└─────────────────────────────────┘
```

### **2. 发布页面布局 (Publish Page)**
```
┌─────────────────────────────────┐
│ [返回] 发布闲置 [发布] │ ← 导航栏
├─────────────────────────────────┤
│ ┌─────────────────────┐ │
│ │ 图片上传区域 │ │
│ │ [＋] [＋] [＋] │ │
│ │ [＋] [＋] │ │
│ └─────────────────────┘ │
├─────────────────────────────────┤
│ 标题 │
│ [请输入物品标题] │
├─────────────────────────────────┤
│ 分类选择 │
│ [请选择分类 →] │
├─────────────────────────────────┤
│ 场景选择 │
│ ○ 仅小区 ○ 仅公司 ○ 双场景 │
├─────────────────────────────────┤
│ 价格设置 │
│ [请输入价格] 元 │
│ ○ 免费互换 ○ 低价转让 │
├─────────────────────────────────┤
│ 新旧程度 │
│ [请选择新旧程度 →] │
├─────────────────────────────────┤
│ 物品描述 │
│ [请详细描述物品状况...] │
└─────────────────────────────────┘
```

### **3. 聊天页面布局 (Chat Page)**
```
┌─────────────────────────────────┐
│ [返回] 用户昵称 │ ← 导航栏
├─────────────────────────────────┤
│ │
│ 对方消息 │
│ [时间] │
│ ┌─────────────┐ │
│ │ 消息内容 │ │
│ └─────────────┘ │
│ │
│ 我方消息 │
│ [时间] │
│ ┌─────────────┐ │
│ │ 消息内容 │ │
│ └─────────────┘ │
│ │
├─────────────────────────────────┤
│ [＋] [输入消息...] [发送] │ ← 输入框
└─────────────────────────────────┘
```

### **4. 个人中心布局 (Profile Page)**
```
┌─────────────────────────────────┐
│ 个人中心 │ ← 导航栏
├─────────────────────────────────┤
│ ┌─────────────────────┐ │
│ │ 用户信息区域 │ │
│ │ [头像] │ │
│ │ 昵称 │ │
│ │ [编辑资料] │ │
│ └─────────────────────┘ │
├─────────────────────────────────┤
│ 我的闲置 │
│ [我的发布 →] [我的收藏 →] │
├─────────────────────────────────┤
│ 交易记录 │
│ [待处理 →] [已完成 →] │
├─────────────────────────────────┤
│ 地址管理 │
│ [小区地址 →] [公司地址 →] │
├─────────────────────────────────┤
│ 积分中心 │
│ [当前积分：XXX] [积分明细 →] │
├─────────────────────────────────┤
│ 设置 │
│ [通知设置] [隐私设置] │
│ [关于我们] [反馈建议] │
└─────────────────────────────────┘
```

## **五、交互与动效规范**

### **1. 页面转场**
- **页面进入**：从右侧滑入，时长300ms
- **页面退出**：向左侧滑出，时长300ms
- **模态弹出**：从底部向上滑入，时长250ms，背景半透明蒙层

### **2. 按钮交互**
```css
/* 悬停效果 */
.btn:hover {
  opacity: 0.9;
}

/* 点击效果 */
.btn:active {
  transform: scale(0.98);
  transition: transform 0.1s ease;
}
```

### **3. 加载状态**
#### **骨架屏 (Skeleton Screen)**
```css
.skeleton {
  background: linear-gradient(
    90deg,
    #F8F8F8 25%,
    #EEEEEE 50%,
    #F8F8F8 75%
  );
  background-size: 200% 100%;
  animation: loading 1.5s infinite;
  border-radius: 4px;
}

@keyframes loading {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

#### **加载动画**
```css
.spinner {
  width: 24px;
  height: 24px;
  border: 2px solid #EEEEEE;
  border-top-color: #07C160;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```

### **4. 下拉刷新**
```css
.refresh-container {
  position: relative;
}

.refresh-indicator {
  height: 50px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: #999999;
  font-size: 13px;
}

.refresh-loading {
  animation: spin 1s linear infinite;
  margin-right: 8px;
}
```

## **六、图标与图片规范**

### **1. 图标库**
- **尺寸**：24x24px (导航图标)，20x20px (操作图标)
- **风格**：线性图标，2px描边，圆角端点
- **颜色**：激活状态 `#07C160`，未激活 `#999999`

### **2. 图片规范**
- **物品图片**：统一1:1比例，最大宽度640px
- **头像**：圆形，64x64px (大)，40x40px (中)，32x32px (小)
- **压缩要求**：所有图片需压缩至webp格式，保证加载速度

### **3. 默认图片**
```css
.default-image {
  background: linear-gradient(135deg, #F5F5F5 0%, #EEEEEE 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #CCCCCC;
}
```

## **七、响应式与适配**

### **1. 屏幕适配**
- 使用rpx单位，以750rpx为设计稿基准
- 重要元素使用安全区域padding：`padding-bottom: env(safe-area-inset-bottom)`

### **2. 断点系统**
```css
/* 小程序内按屏幕宽度适配 */
@media screen and (max-width: 320px) {
  .container { padding: 12px; }
}

@media screen and (min-width: 375px) {
  .container { padding: 16px; }
}

@media screen and (min-width: 414px) {
  .container { padding: 20px; }
}
```

## **八、设计资源引用**

### **建议使用的设计工具**
- **设计稿**：使用750x1334px (iPhone 8) 作为基准尺寸
- **标注工具**：Zeplin或Pixso标注间距、颜色、字号
- **图标资源**：建议使用阿里图标库或FontAwesome

### **开发实现建议**
```css
/* 全局CSS变量，便于维护 */
:root {
  --primary-color: #07C160;
  --secondary-color: #2A6EFF;
  --text-primary: #333333;
  --text-secondary: #666666;
  --bg-color: #F8F8F8;
  --border-color: #EEEEEE;
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --radius-sm: 4px;
  --radius-md: 8px;
}
```

## **九、验收标准**

1. **视觉一致性**：所有页面遵循本规范的颜色、间距、字体
2. **交互流畅性**：所有点击、滑动操作有明确反馈
3. **加载性能**：首屏加载时间不超过2秒
4. **适配完整性**：在主流手机屏幕尺寸上显示正常
5. **无障碍支持**：重要操作区域有足够点击区域（最小44x44px）

---

**文档版本**：v1.0  
**更新日期**：2024年  
**适用项目**：社区闲置快换小程序  
**设计原则**：在保持规范的前提下，可根据具体场景微调，但需保持整体风格统一
